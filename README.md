# Task Manager REST API

A RESTful API for task management with user authentication using Node.js, Express, and MongoDB.

## Setup

1. Install dependencies: `npm install`
2. Copy `.env.example` to `.env` and update with your MongoDB URI and JWT secret.
3. Run in development: `npm run dev`
4. Run tests: `npm test`